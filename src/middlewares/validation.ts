// import { FastifyRequest, FastifyReply } from 'fastify';

// /**
//  * Middleware to validate query parameters
//  * Similar to Express.js middleware pattern
//  */
// export const hostAndOriginCheck = async (request: FastifyRequest, reply: FastifyReply) => {
//     console.log()

//     // If validation passes, continue to next handler
//     // In Fastify, we don't call next(), we just return nothing
// };

